[package]
name = "sempal-analysis-admin"
version = "19.0.0"
edition = "2024"

[dependencies]
hdbscan = "0.12.0"
rusqlite = { version = "0.37.0", features = ["bundled", "load_extension"] }
sempal = { path = "../.." }
