use super::*;
use crate::app::controller::{LoadedAudio, test_support};
use std::path::PathBuf;

mod collapse_recovery;
mod crossfade_handles;
mod curve_edits;
mod edit_selection;
mod fade_top_handles;
mod mute_handles;
