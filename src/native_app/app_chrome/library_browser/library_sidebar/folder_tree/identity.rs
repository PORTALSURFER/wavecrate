pub(super) fn folder_row_key(folder_id: &str) -> String {
    format!("folder-row-{folder_id}")
}

pub(super) fn folder_row_hit_key(folder_id: &str) -> String {
    format!("folder-row-hit-{folder_id}")
}
