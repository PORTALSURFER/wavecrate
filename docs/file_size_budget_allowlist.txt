# Allowlist for the Rust file size budget check.
#
# Sempal aims to keep source files under 400 LOC. This allowlist captures the
# current legacy exceptions within the guardrail scope (`src/`, `tests/`, and
# `vendor/radiant/src`) that still exceed the budget on 2026-03-16 so the
# check can enforce the limit on new or edited files without blocking
# incremental cleanup work.
#
# Prefer shrinking or splitting these files over time and delete entries once
# they fall back under the budget.

