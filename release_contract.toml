app_name = "wavecrate"
repo_slug = "PORTALSURFER/wavecrate"
channels = ["nightly", "stable"]
targets = [
  "x86_64-pc-windows-msvc"
]

[platform_labels]
windows = "windows"
linux = "linux"
macos = "macos"
x86_64 = "x86_64"
aarch64 = "aarch64"

[templates]
stable_asset = "{APP_NAME}-v{VERSION}-{platform}-{arch}.zip"
nightly_asset = "{APP_NAME}-nightly-{platform}-{arch}.zip"
stable_checksums = "checksums-v{VERSION}.txt"
nightly_checksums = "checksums-nightly.txt"
stable_checksums_sig = "checksums-v{VERSION}.txt.sig"
nightly_checksums_sig = "checksums-nightly.txt.sig"

[archive_layout]
root_dir = "{APP_NAME}"
files = ["{APP_NAME}.exe", "models/panns_cnn14_16k.bpk", "update-manifest.json"]
optional_dirs = ["resources"]
